# File: .env.example

# Server Configuration
# Port for local development (optional - SvelteKit defaults to 5173)
PORT=4332

# Database Connection (Neon Postgres)
# Pooled connection - used by the application for queries
DATABASE_URL=postgresql://<USERNAME>:<PASSWORD>@<HOST>-pooler.<REGION>.aws.neon.tech/<DATABASE>?sslmode=require

# Direct (unpooled) connection - used for migrations and schema changes
DATABASE_URL_UNPOOLED=postgresql://<USERNAME>:<PASSWORD>@<HOST>.<REGION>.aws.neon.tech/<DATABASE>?sslmode=require

# AI Analysis (Gemini API)
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Notes:
# - For Neon database credentials, visit: https://console.neon.tech
# - Copy "Connection string (pooled)" for DATABASE_URL
# - Copy "Connection string (direct)" for DATABASE_URL_UNPOOLED
# - When deploying to Vercel, Vercel auto-creates POSTGRES_URL (our code supports both names)
