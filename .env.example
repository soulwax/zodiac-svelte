# Replace with your DB credentials!
PORT=4332
DB=db
DB_USER=db_user
DB_PASSWORD=db_pass
DB_HOST="localhost"
DB_PORT=5432
DB_OPTIONS="?sslmode=require" # and other optios if necessary, like certfiles
DATABASE_URL="postgres://"
# OpenAI API Key for generating astrological analysis
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Database connection (if using PostgreSQL)
# DATABASE_URL=postgresql://user:password@localhost:5432/zodiac
